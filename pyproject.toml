[tool.poetry]
name = "strainr"
version = "0.1.0"
description = "Reference-based strain classifier based off k-mers of interest"
authors = ["mladen5000 <mladen.rasic@gmail.com>"]
license = "BSD"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.0.3"
biopython = "^1.81"
ncbi-genome-download = "tqdm"
numpy = "^1.25.1"
ncbi-genome-download = "^0.3.1"
tqdm = "^4.65.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"